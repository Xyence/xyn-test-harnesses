id: owner_lookup_owner_unavailable_behavior
suite: deal-finder-owner-lookup
title: "Owner Lookup: Owner Unavailable Behavior"
request: >
  Return an explicit owner_unavailable state when parcel resolution succeeds but owner evidence is missing.
expected_artifacts:
  - xyn-ui
expected_primary_artifact: xyn-ui
optional_artifacts:
  - xyn-api
forbidden_artifacts: []
accepted_dependency_reasons:
  - ui_command_requires_api_route_registration
planner_expectations:
  should_use_mcp: true
  may_require_core_xyn_changes: false
  success_criteria:
    - Owner-unavailable state is explicit and not a transport failure.
  required_phrases: []
  forbidden_phrases: []
deployment:
  mode: preview
  requires_xyn_api: true
  require_branch_isolation: false
assertions:
  expected_operations:
    - owner_lookup_owner_unavailable_processed
  expected_response_fields:
    - path: submitRequest.endpointTarget.submitEndpoint
      contains: "/mcp"
