id: phase1_negative_ui_copy_only_no_api
title: "Phase1 Negative Control: UI Copy Only, No API Widening"
request: >
  Update Deal Finder helper text shown in the command palette results panel only,
  explicitly avoiding any command execution or datasource persistence changes.
expected_artifacts:
  - xyn-ui
expected_primary_artifact: xyn-ui
optional_artifacts:
  - deal-finder-harness
forbidden_artifacts:
  - xyn-api
accepted_dependency_reasons: []
artifact_selection_differ_group: phase1-api-widening-contrast-2
planner_expectations:
  should_use_mcp: true
  should_update_command_palette: true
  may_require_core_xyn_changes: false
  success_criteria:
    - Planner keeps scope in xyn-ui.
    - Planner explicitly avoids backend/API updates.
  required_phrases:
    - xyn-ui
    - validation
  forbidden_phrases:
    - xyn-api
    - api route
deployment:
  mode: preview
  requires_xyn_api: false
  require_branch_isolation: true
ui_checks:
  - type: command_palette_command_present
    command_text: "Deal Finder"
