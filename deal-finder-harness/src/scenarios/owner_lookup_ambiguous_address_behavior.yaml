id: owner_lookup_ambiguous_address_behavior
suite: deal-finder-owner-lookup
title: "Owner Lookup: Ambiguous Address Handling"
request: >
  Resolve parcel identity for an ambiguous address and return a clear low-confidence or ambiguous status.
expected_artifacts:
  - xyn-ui
expected_primary_artifact: xyn-ui
optional_artifacts:
  - xyn-api
forbidden_artifacts: []
accepted_dependency_reasons:
  - ui_command_requires_api_route_registration
planner_expectations:
  should_use_mcp: true
  may_require_core_xyn_changes: false
  success_criteria:
    - Ambiguous or low-confidence address behavior is explicit and machine-readable.
  required_phrases: []
  forbidden_phrases: []
deployment:
  mode: preview
  requires_xyn_api: true
  require_branch_isolation: false
assertions:
  expected_operations:
    - owner_lookup_ambiguous_processed
  expected_response_fields:
    - path: submitRequest.endpointTarget.submitEndpoint
      contains: "/mcp"
